machine:
    services:
        - docker

dependencies:
    override:
      - ./test.sh count

test:
    override:
        - bash circle-test.sh:
            parallel: true
            # Race tests using 960s timeout
            timeout: 1500
